# REFERENCE Compliance Recheck 2026-03-29 00:04:09 +0800

## 动作
- 对照 `docs/reference/01-反屎山AI研发执行总纲（Codex专用浓缩对照版）.md` 与 `docs/reference/02-仓库卫生与命名规范.md` 复核近期推进。
- 复核对象覆盖根目录、`docs/round_logs/`、`tools/checks/check_public_docs.py`、最近提交链到 `dded6d8`。
- 对比既有审计 `docs/REFERENCE_COMPLIANCE_AUDIT_20260328.md`，确认之前结论是否仍成立。

## 结果
- 结论不变：若后续要严格遵守 `docs/reference/`，之前执行 **需要整改**。
- 总体难度仍为 **中等偏高**。
- 代码推进纪律总体稳定，真正需要整改的主战场仍是 **仓库卫生、留痕路径、自动化绑定路径**。

## 新增确认点

### 1. 既有偏离仍然存在
- 根目录仍保留 `MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。
- 留痕日志仍持续写入 `docs/round_logs/`，目前已有 131 份。
- `tools/checks/check_public_docs.py` 仍直接绑定根目录 `MVP_PROGRESS.md` 与 `PUSH_LOG.md`。

### 2. 严格目录锁定当前还缺一块依据
- `docs/reference/02-仓库卫生与命名规范.md` 提到目录结构是否可改，应以 `docs/30-方案/02-V4-目录锁定清单.md` 为准。
- 当前仓库中未找到该文件。
- 这意味着：后续即使想严格宣称“完全按目录锁定清单执行”，目前也缺少一个可核对的正式锁定源。

### 3. 最近安全抽象层预留位本身不算主要整改对象
- `specs/spi/keystore/`、`specs/spi/boot-integrity/`、`specs/spi/storage-encryption/` 已落在 `specs/` 真源体系内部，语义合理。
- 这三组目录来自明确任务指令，且只放接口文档与占位 JSON，没有引入运行时代码或依赖。
- 如果后续要把“目录锁定”执行到最严，还需要补一份目录准入依据，确认这些目录位属于批准结构，而不是把它们再搬走。

## 是否整改
- 要整改。
- 但不建议把“规范整改”和“业务推进”混成一刀大手术。

## 难度分级

### 低难度：立即止血
- 从下一轮开始，不再新增新的根目录台账品类。
- 不再继续扩张 `docs/round_logs/` 这类已知偏离路径。
- 新的制度/审计文档继续放在 `docs/` 语义化文件，不再混入历史习惯路径。

### 中难度：自动化去旧路径绑定
- 把 `tools/checks/check_public_docs.py` 对根目录台账的强绑定改成新路径或索引路径。
- 给新台账位置补统一入口，避免文档、脚本、协作口径各写一套。

### 中高难度：历史材料迁移
- 将 `docs/round_logs/` 历史材料迁移到合规路径。
- 处理 `PUSH_LOG.md` 里的历史引用。
- 收口根目录三份主台账的迁移与引用修复。

### 高难度：没有锁定清单就先全面重排目录
- 当前不建议直接做。
- 因为缺少 `docs/30-方案/02-V4-目录锁定清单.md`，一旦硬迁，容易从“整改”变成“再次越界改结构”。

## 建议下一步
1. 先补目录锁定依据：补出或确认 `docs/30-方案/02-V4-目录锁定清单.md` 的正式版本。
2. 立一个单独的“仓库卫生整改”小任务，只做路径与自动化迁移，不混入业务开发。
3. 在整改任务开始前，先执行“止血策略”：
   - 不再新增根目录台账种类
   - 不再扩张 `docs/round_logs/`
   - 新审计类材料统一放 `docs/` 语义化文件

## 一句话判断
- 之前执行不是乱推进，代码层面大体稳；
- 但若以后要严格按 `docs/reference/`，**要整改，而且主要是仓库卫生整改，不是重做最近功能切片**。

# V4 台账索引真源记录 2026-03-29 00:52:22 +0800

## 动作
- 新建 docs/30-方案/08-V4-ledger-index-manifest.json，把三份主台账的逻辑标识、新旧路径、读取顺序、写入模式、切换状态落成可机读真源。
- 本轮仍不创建 docs/records/，也不迁移任何历史台账文件。
- 同步文档索引、目录锁定清单与公开文档门禁，让后续脚本改造可以直接依赖这份 manifest。

## 结果
- Slice A 从“规则文档”升级成“规则文档 + 最小机读真源”。
- 后续做索引驱动读取时，不再需要先把规则从 Markdown 手工翻译成数据结构。
- 这让下一刀可以直接围绕 manifest 做最小读取实现。

## 理由
- 这一步短期更难，因为先做结构化真源而不是直接迁移文件。
- 但长期收益更高：后面无论是脚本、检查还是迁移流程，都能复用同一份 manifest，减少口径漂移。

## 下一步
- 下一刀优先做 manifest 的最小读取入口，让脚本先能读这个索引真源。
- 未完成读取入口前，不直接迁移 开发计划.md、MVP_PROGRESS.md、PUSH_LOG.md。

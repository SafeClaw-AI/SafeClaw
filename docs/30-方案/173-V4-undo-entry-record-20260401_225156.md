# V4 undo entry record

- Time: 2026-04-01 22:51:56 +0800
- Slice: M2 Slice 299
- Action: First extended `tools/checks/check_examples_smoke.py` with a failing `safeclaw-mvp-undo` case and added one focused `undo` parser contract in `safeclaw-sqlite/examples/safeclaw_mvp_entry.rs`; also normalized the zero-guard rollback assertion in `safeclaw-core/src/worker_lifecycle.rs`, then synced `开发计划.md` and `tests/contracts/test_chancellor_panel.py` to the new `M2-P0-4` truth after the `archive-note -> undo` path was verified end to end.
- Code Closure: This round closes `M2-P0-3` on the smallest real user path only: one archived note can be created, shown in a readable bill, then removed by `undo`; it does not yet widen to wrapper-wide acceptance or GUI delivery.
- Contracts: The new smoke case now fail-closes on `undo target`, `undo result`, the reversed bill line `已撤销`, and `output exists => false`; the example also has one focused parser test that locks `undo` task/db/output context.
- Result: SafeClaw now has a truthful `archive-note -> undo` value loop: users can create a real archived note, read what changed, execute `undo`, and see the file disappear again with the bill switched to rollback wording.
- Verify: `cargo +stable-x86_64-pc-windows-gnu test -p safeclaw-core rollback -- --nocapture`, `cargo +stable-x86_64-pc-windows-gnu test -p safeclaw-core --test protocol_contracts worker_transition_table_covers_full_spec_paths -- --nocapture`, `cargo +stable-x86_64-pc-windows-gnu test -p safeclaw-sqlite --example safeclaw_mvp_entry --quiet`, `python -X utf8 tools/checks/check_examples_smoke.py`.
- Why: Compared with继续扩 wrapper/UI 面，先把最小真实任务的撤销闭环锁成可验证事实更稳，因为它直接回答了“做了事后能不能完整撤回”这个普通用户最关心的问题。
- Next: Start `M2-P0-4 产品价值闭环验收` by locking one stable acceptance path for `真实任务 -> 可读账单 -> undo -> 完整还原`.

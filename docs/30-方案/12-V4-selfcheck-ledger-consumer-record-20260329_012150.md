# V4 自检台账消费记录 2026-03-29 01:21:50 +0800

## 动作
- 更新 	ools/checks/selfcheck.py，新增 Ledger index manifest 检查阶段。
- 让 selfcheck 在进入其余大检查前，先显式运行 	ools/checks/ledger_index_manifest.py。
- 本轮不扩大到其他脚本，不迁移任何台账文件，只把第二个消费点接上索引真源。

## 结果
- selfcheck.py 已成为第二个真实消费台账 manifest 的消费点。
- 后续即使全量自检在其他历史问题处失败，台账索引检查也会先明确执行。
- 这让 manifest 不再只服务单一门禁，而开始进入总检查入口。

## 理由
- 这是最小且长期收益高的第二消费点：改动小、验证快、对后续路径迁移有持续价值。
- 先把 manifest 拉进总入口，比继续堆单点脚本更利于后面统一收敛。

## 下一步
- 下一刀可继续找第三个消费点，或开始为 manifest 增加可复用的路径解析辅助。
- 在至少两个消费点都稳定运行后，再考虑真正迁移某一份台账。

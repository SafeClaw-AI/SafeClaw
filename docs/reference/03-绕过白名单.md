# 绕过白名单

> 本白名单只登记当前无法立即移除、且已有明确理由的绕过点。默认策略仍是 fail-closed；任何新增 `# noqa`、warning filter、skip、`type: ignore`、`nolint` 等绕过，若未先登记到本表，门禁必须直接失败。
> 当前原则：优先移除低价值绕过；只对白名单内的最小剩余集放行。
> 当前状态：已清零，现无登记中的有效绕过点。

## 绕过白名单

| 对象标识 | 绕过类型 | 标记 | 责任人 | 复查周期 | 原因 |
| --- | --- | --- | --- | --- | --- |

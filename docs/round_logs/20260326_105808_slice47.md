# Slice 47 · hibernated resume 真入口

- 时间：2026-03-26 10:57:58 +0800
- 动作：补 `safeclaw_mvp_entry` 的 `seed-hibernated` / `resume`，wrapper 补 `service-resume`，并把 `service-status` 的 hibernated `next_command` 切到 `service-resume --report`。
- 结果：本地 MVP 现在可以从 `hibernated` 现场直接走 `seed-hibernated -> service-resume -> service-status -> report` 真闭环；不再只是看见 winter 现场，却还得停在人工猜命令。
- 理由：`Slice 46` 只做到了“看得见 hibernated”；这轮最小真缺口就是“看到了以后怎么继续”。
- 验证：`python -m py_compile tools/mvp/safeclaw_mvp.py tools/checks/check_tooling_smoke.py`、`python tools/checks/check_tooling_smoke.py`、`python tools/checks/check_mvp_operator_flow.py`、`python tools/checks/selfcheck.py`
- 代码提交：`528b58c feat: add hibernated resume operator path`
- 下一步：`Slice 48` 把 `seed-hibernated -> service-resume -> service-status -> report` 真链路升成 `operator-flow` 门禁，不只留在 smoke。

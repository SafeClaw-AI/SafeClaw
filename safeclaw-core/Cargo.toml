[package]
name = "safeclaw-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
publish = false
description = "SafeClaw Rust core scaffold aligned to protocol specs"

[lib]
name = "safeclaw_core"
path = "src/lib.rs"

[dependencies]

# 开发计划

最后更新时间：2026-03-26 07:05:55 +0800
当前基线提交：`9c0f2cd`（Slice 36 功能闭环基线）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环

## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 36 刀，最新已落地 `Slice 36`：组合动作 preflight 顶层 `error.requires_sidecar`。
- `preflight --action ai-reason` 已稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，并暴露 `requires_model=true` / `requires_sidecar=true` / `error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- `service-status` 已同步回显 top-level `runtime_profile` / `model_provider` / `sidecar` / `offline_gate`。
- `demo` / `service-run` / `service-retry` / `service-recover` / `service-reconcile` 现支持 `--preflight-action <name>`，可在组合动作里复用不同 preflight 合同。
- 当组合动作因 preflight 被阻断时，`error.details.preflight` 会稳定保留完整门禁载荷；同时会在顶层 `error.code` 镜像 `preflight-blocked`、在顶层 `error.reason` 镜像既有 `preflight_reason`、在顶层可选 `error.error_code` 镜像既有 `preflight_error_code`、在顶层可选 `error.degradation_mode` 镜像既有 `degradation_mode`、在顶层 `error.requires_model` 镜像既有 `requires_model`、在顶层 `error.requires_sidecar` 镜像既有 `requires_sidecar`、在顶层 `error.summary` 镜像既有 `preflight_summary`、在顶层 `error.requested_action` 镜像既有 `preflight_requested_action`，并镜像 `preflight_requested_action` / `preflight_reason` / `preflight_summary` / 可选 `preflight_error_code` 到 `error.details` 顶层。
- 如使用 `--preflight-action ai-reason`，则 `error.details.preflight.error_code`、`error.details.preflight_error_code` 与顶层可选 `error.error_code` 都稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，顶层 `error.degradation_mode` 则稳定返回 `provider_unavailable`，顶层 `error.requires_model` / `error.requires_sidecar` 则分别稳定返回 `true/false`。
- `target/mvp` 是共享验证状态目录；`check_tooling_smoke.py` 与 `check_mvp_operator_flow.py` 必须线性顺跑，不能并发。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。

## 二、当前状态
- 最新完成：组合动作的 preflight 阻断错误现会在顶层 `error.requires_sidecar` 稳定回显既有 `requires_sidecar`，脚本不必再深入嵌套 `error.details.preflight` 载荷就能拿到组合级 sidecar 依赖语义。
- 当前最重要的已验证路径：`service-run --preflight --preflight-action ai-reason --json` 与 `demo --preflight --preflight-action ai-reason --json` 都会稳定返回顶层 `error.code=preflight-blocked`、顶层 `error.reason=ERR_AI_PROVIDER_UNAVAILABLE`、顶层 `error.error_code=ERR_AI_PROVIDER_UNAVAILABLE`、顶层 `error.degradation_mode=provider_unavailable`、顶层 `error.requires_model=true`、顶层 `error.requires_sidecar=true`、顶层 `error.summary=<preflight_summary>`、顶层 `error.requested_action=ai-reason`，以及 `error.details.preflight_requested_action=ai-reason`、`preflight_reason=ERR_AI_PROVIDER_UNAVAILABLE`、`preflight_error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- 权限阻断路径也已同步镜像：如 `service-run --enforce-permission --json`，会稳定返回顶层 `error.code=preflight-blocked`、顶层 `error.reason=write_scope_requires_confirmation`、顶层 `error.degradation_mode=local_only_ok`、顶层 `error.requires_model=false`、顶层 `error.requires_sidecar=false`、顶层 `error.summary=<preflight_summary>`、顶层 `error.requested_action=service-run`、`preflight_requested_action=service-run` 与 `preflight_reason=write_scope_requires_confirmation`，且不会伪造顶层 `error.error_code` 或 `error.details.preflight_error_code`。
- 现在的主视图：`doctor` 看运行环境，`service-status` 看治理快照 + 运行边界 + 离线阻断提示，`preflight` 看单动作门禁细节，组合动作错误 JSON 看顶层 `error.code` / `error.reason` / 可选 `error.error_code` / `error.degradation_mode` / `error.requires_model` / `error.requires_sidecar` / `error.summary` / `error.requested_action` + preflight 阻断细节与快捷字段。
- 当前 `ai-reason` 仍是 preflight-only 占位动作，不是真实执行入口。

## 三、下一候选（按优先级排序）
1. `Slice 37`：盘点 combo 阻断错误剩余既有字段差异
   - 目标：对照顶层 `error.*` 与 `error.details.preflight` 既有合同，确认“镜像链”是否已经收口，避免在已闭环区域盲目继续加字段。
   - 前提：先做字段盘点与价值排序；若无高价值缺口，不扩展新语义，不改 Rust。
2. 若镜像链已收口，再切回 `M1b` 其他生存层空白
   - 原因：当前 provider-unavailable 合同已经覆盖 `preflight -> service-status -> combo error.details.preflight -> combo shallow shortcuts -> combo error.code -> combo error.reason -> combo error.error_code -> combo error.degradation_mode -> combo error.requires_model -> combo error.requires_sidecar -> combo error.summary -> combo error.requested_action` 十二个视角，下一步应先确认是否还有“必须镜像的既有字段”，再决定是否切换主线。

## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 多 Worker 并发与 scope-level mutex
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 验证顺序固定：`py_compile -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`

## 七、下一刀完成判据
- 完成一轮 combo blocked error 顶层 `error.*` 与嵌套 `error.details.preflight` 的字段差异盘点，并明确是否还存在高价值既有字段缺口。
- 若确认镜像链已收口，开发计划中要显式写出“镜像链闭环”再切回 `M1b` 其他生存层空白。
- 如继续推进实现，相关文本 / JSON / smoke / docs 不回归。

## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀不是 `Slice 37`，必须先在本文件补一行“为什么改计划”。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。





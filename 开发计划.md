# 开发计划

最后更新时间：2026-03-30 11:21:47 +0800
当前基线提交：`b2c3a26`（recover stale mvp lock on windows 代码基线）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环
当前阶段：已进入 M1b，前 247 刀已完成；最近六十轮继续沿 reference fail-closed 主线收口 broad exception family、helper 真源、消息真源、caught_types 真源、高风险异常真源与 silent fallback 语法糖真源
## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 189 刀；`Slice 40` 做完 budget/operator 盘点，`Slice 41` 锁住了并发 happy-path 基线，`Slice 42` 把 `executed_assumed -> service-reconcile` 自隔离收口路径锁进了 operator-flow，`Slice 43` 把 `active lease -> stalled` 等待路径补进了 operator-flow，`Slice 44` 把 peer quarantine / contended 两条 peer 边缘路径锁进了 operator-flow，`Slice 45` 确认 budget 仍无真实 runtime/source 并补上 no-fake-budget 护栏，`Slice 46` 把 hibernated 现场补成了显式可读提示，`Slice 47` 把 hibernated 现场继续补成了真实 `resume` / `service-resume` 入口，`Slice 48` 把这条冬眠收口链继续升成了 operator-flow 真门禁，`Slice 49` 补齐接错现场失败合同，`Slice 50` 锁住 `service-resume` 缺上下文合同，`Slice 51` 锁住 `service-reconcile` 缺上下文合同，`Slice 52` 锁住原生 `reconcile` 缺上下文合同，`Slice 53` 锁住原生 `resume` 缺上下文合同，`Slice 54` 锁住 `cmd resume` 缺上下文合同，`Slice 55` 锁住 `cmd reconcile` 缺上下文合同，`Slice 56` 锁住 `ps1 resume` 缺上下文合同，`Slice 57` 锁住 `ps1 reconcile` 缺上下文合同，`Slice 58` 锁住 `ps1 report` invalid-json remembered-session 合同，`Slice 59` 锁住 `ps1 retry` invalid-json remembered-session 合同，`Slice 60` 锁住 `ps1 session` invalid-json 合同，`Slice 61` 锁住 `ps1 sessions` invalid-limit 合同，`Slice 62` 锁住 `cmd doctor` missing-value-after-db 合同，`Slice 63` 锁住 `cmd verify` invalid-json 合同，`Slice 64` 锁住 `ps1 verify` invalid-json 合同，`Slice 65` 再把 `cmd recover` invalid-json remembered-session 合同锁进 smoke，`Slice 66` 再把 `ps1 demo` fail-json remembered-session 合同锁进 smoke，`Slice 67` 再把 `ps1 recover-demo` fail-json remembered-session 合同锁进 smoke，`Slice 68` 再把 `ps1 retry-demo` fail-json remembered-session 合同锁进 smoke，`Slice 69` 再把 `ps1 status` invalid-json remembered-session 合同锁进 smoke，`Slice 70` 再把 `cmd status` missing-value-after-db remembered-session 合同锁进 smoke，`Slice 71` 再把 `cmd report` missing-task-context 合同锁进 smoke，`Slice 72` 再把 `cmd retry` missing-task-context 合同锁进 smoke，`Slice 73` 再把 `ps1 recover` missing-task-context 合同锁进 smoke，`Slice 74` 再把 `cmd service-run` invalid-limit 合同锁进 smoke，`Slice 75` 再把 `ps1 service-run` invalid-limit 合同锁进 smoke，`Slice 76` 再把 `cmd service-retry` invalid-limit 合同锁进 smoke，`Slice 77` 再把 `ps1 service-retry` invalid-limit 合同锁进 smoke，`Slice 78` 再把 `cmd service-recover` invalid-limit 合同锁进 smoke，`Slice 79` 再把 `ps1 service-recover` invalid-limit 合同锁进 smoke，`Slice 80` 再把 `cmd service-status` invalid-limit 合同锁进 smoke，`Slice 81` 再把 `ps1 service-status` invalid-limit 合同锁进 smoke，`Slice 82` 再把 `ps1 service-status` 成功 JSON 合同锁进 smoke，`Slice 83` 再把 `ps1 service-run` 成功 JSON 合同锁进 smoke，`Slice 84` 再把 `ps1 service-run --report` 成功 JSON 合同锁进 smoke，`Slice 85` 再把 `ps1 service-retry` 成功 JSON 合同锁进 smoke，`Slice 86` 再把 `ps1 service-retry --report` 成功 JSON 合同锁进 smoke，`Slice 87` 再把 `ps1 service-recover` 成功 JSON 合同锁进 smoke，`Slice 88` 再把 `ps1 service-recover --report` 成功 JSON 合同锁进 smoke，`Slice 89` 再把 `ps1 service-resume` 成功 JSON 合同锁进 smoke，`Slice 90` 再把 `ps1 service-resume --report` 成功 JSON 合同锁进 smoke，`Slice 91` 再把 `ps1 service-reconcile` 成功 JSON 合同锁进 smoke，`Slice 92` 再把 `ps1 service-reconcile --report` 成功 JSON 合同锁进 smoke，`Slice 93` 再把 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 94` 再把 `ps1 retry` 成功 JSON 合同锁进 smoke，`Slice 95` 再把 `ps1 recover` 成功 JSON 合同锁进 smoke，`Slice 96` 再把 `ps1 status` 显式成功 JSON 合同锁进 smoke，`Slice 97` 再把 `ps1 report` 显式成功 JSON 合同锁进 smoke，`Slice 98` 再把 crash 现场 `ps1 status` 显式成功 JSON 合同锁进 smoke，`Slice 99` 再把 crash 现场 `ps1 report` 显式成功 JSON 合同锁进 smoke，`Slice 100` 再把 crash 连续性下的 `ps1 session` 成功 JSON 合同锁进 smoke，`Slice 101` 再把 crash 连续性下的 `ps1 sessions` 成功 JSON 合同锁进 smoke，`Slice 102` 再把 crash 会话连续性下无参 `ps1 status` 成功 JSON 合同锁进 smoke，`Slice 103` 再把 crash 会话连续性下无参 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 104` 再把 crash 会话连续性下无参 `ps1 recover` 成功 JSON 合同锁进 smoke，`Slice 105` 再把 failed 会话连续性下无参 `ps1 retry` 成功 JSON 合同锁进 smoke，`Slice 106` 再把 failed 会话连续性下无参 `ps1 status` 成功 JSON 合同锁进 smoke，`Slice 107` 再把 failed 会话连续性下无参 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 108` 再把 failed 会话连续性下无参 `ps1 session` 成功 JSON 合同锁进 smoke，`Slice 109` 再把 failed 会话连续性下无参 `ps1 sessions` 成功 JSON 合同锁进 smoke，`Slice 110` 再把 failed 显式上下文 `ps1 status --db ... --task-id ... --json` 成功 JSON 合同锁进 smoke，`Slice 111` 再把 failed 显式上下文 `ps1 report --db ... --task-id ... --json` 成功 JSON 合同锁进 smoke，`Slice 112` 再把无参 `cmd status --json` 成功 JSON 合同锁进 smoke。，`Slice 113` 再把无参 `cmd session --json` 成功 JSON 合同锁进 smoke。，`Slice 114` 再把无参 `cmd sessions --json` 成功 JSON 合同锁进 smoke。，`Slice 115` 再把无参 `cmd retry --json` 成功 JSON 合同锁进 smoke。，`Slice 116` 再把无参 `cmd recover --json` 成功 JSON 合同锁进 smoke。，`Slice 117` 再把 failed 显式上下文 `cmd status --json` 成功 JSON 合同锁进 smoke，`Slice 118` 再把 failed 显式上下文 `cmd report --json` 成功 JSON 合同锁进 smoke，`Slice 119` 再把 failed 会话连续性下无参 `cmd status --json` 成功 JSON 合同锁进 smoke，`Slice 120` 再把 failed 会话连续性下无参 `cmd report --json` 成功 JSON 合同锁进 smoke，`Slice 121` 再把 failed 会话连续性下无参 `cmd session --json` 成功 JSON 合同锁进 smoke，`Slice 122` 再把 failed 会话连续性下无参 `cmd sessions --json` 成功 JSON 合同锁进 smoke，`Slice 123` 再把 `cmd preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 124` 再把 `cmd workspace --name demo --json` 成功 JSON 合同锁进 smoke，`Slice 125` 再把 `cmd seed-failed --json` 成功 JSON 合同锁进 smoke，`Slice 126` 再把 `cmd seed-crash --json` 成功 JSON 合同锁进 smoke，`Slice 127` 再把 `cmd seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 128` 再把 `cmd workspace --clear --json` 成功 JSON 合同锁进 smoke，`Slice 129` 再把 `cmd forget --json` 成功 JSON 合同锁进 smoke，`Slice 130` 再把原生 `resume --json` 成功 JSON 合同锁进 smoke，`Slice 131` 再把 `cmd resume --json` 成功 JSON 合同锁进 smoke，`Slice 132` 再把根入口 `safeclaw.cmd resume --json` 成功 JSON 合同锁进 smoke，`Slice 133` 再把根入口 `safeclaw.cmd seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 134` 再把根入口 `safeclaw.ps1 seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 135` 再把根入口 `safeclaw.ps1 resume --json` 成功 JSON 合同锁进 smoke，`Slice 136` 再把根入口 `safeclaw.ps1 seed-failed --json` 成功 JSON 合同锁进 smoke，`Slice 137` 再把根入口 `safeclaw.ps1 seed-crash --json` 成功 JSON 合同锁进 smoke，`Slice 138` 再把根入口 `safeclaw.ps1 workspace --name readme-root-ps1 --json` 成功 JSON 合同锁进 smoke，`Slice 139` 再把根入口 `safeclaw.ps1 doctor --json` 成功 JSON 合同锁进 smoke，`Slice 140` 再把根入口 `safeclaw.ps1 workspace --clear --json` 成功 JSON 合同锁进 smoke，`Slice 141` 再把根入口 `safeclaw.ps1 forget --json` 成功 JSON 合同锁进 smoke，`Slice 142` 再把根入口 `safeclaw.ps1 preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 143` 再把根入口 `safeclaw.cmd preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 144` 再把根入口 `safeclaw.ps1 verify --json` 成功 JSON 合同锁进 smoke，`Slice 145` 再把根入口 `safeclaw.ps1 workspace --json` 默认态 JSON 合同锁进 smoke，`Slice 146` 再把根入口 `safeclaw.cmd workspace --json` 默认态 JSON 合同锁进 smoke，`Slice 147` 再把根入口 `safeclaw.cmd doctor --json` 默认态 JSON 合同锁进 smoke，`Slice 148` 再把根入口 `safeclaw.cmd service-status --limit 5 --json` 默认态 JSON 快照合同锁进 smoke，`Slice 149` 再把根入口 `safeclaw.ps1 service-status --limit 5 --json` 默认态 JSON 快照合同锁进 smoke，`Slice 150` 再把根入口 `safeclaw.cmd preflight --action ai-reason --json` 的 provider-unavailable deny JSON 合同锁进 smoke，`Slice 151` 再把根入口 `safeclaw.ps1 preflight --action ai-reason --json` 的 provider-unavailable deny JSON 合同锁进 smoke，`Slice 152` 再把根入口 `safeclaw.cmd preflight --action demo --json` 的 allow JSON 合同锁进 smoke，`Slice 153` 再把根入口 `safeclaw.ps1 preflight --action demo --json` 的 allow JSON 合同锁进 smoke，`Slice 154` 再把根入口 `safeclaw.ps1 service-run --json` 的成功 JSON 合同锁进 smoke，`Slice 155` 再把根入口 `safeclaw.ps1 service-retry --json` 的成功 JSON 合同锁进 smoke，`Slice 156` 再把根入口 `safeclaw.ps1 service-recover --json` 的成功 JSON 合同锁进 smoke，`Slice 157` 再把根入口 `safeclaw.cmd service-reconcile --json` 的成功 JSON 合同锁进 smoke，`Slice 158` 再把根入口 `safeclaw.cmd service-resume --json` 的成功 JSON 合同锁进 smoke，`Slice 159` 再把根入口 `safeclaw.ps1 service-resume --json` 的成功 JSON 合同锁进 smoke，`Slice 160` 再把根入口 `safeclaw.ps1 service-reconcile --json` 的成功 JSON 合同锁进 smoke。
- `preflight --action ai-reason` 已稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，并暴露 `requires_model=true` / `requires_sidecar=true` / `error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- 当前新增收口：`service-status` 顶层 `heartbeat` 已严格 live-only；同时顶层 `coordination` 在 remembered session 命中的当前任务仍在 recent window 内时，会优先跟随当前任务，不再被更近历史行抢走摘要。
- `service-status` 现在已经能稳定回显 `runtime_profile` / `model_provider` / `sidecar` / `offline_gate` / `heartbeat` / `coordination` / `recent_tasks`。
- 经 `Slice 40` + `Slice 45` 两次复查，蓝图里的财务熔断 / 预算状态目前还只在蓝图、specs 和错误码里；现有 wrapper/operator 视图仍没有真实 `budget` 数据源可直接复用。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。

## 二、当前状态
- 最新完成：`Slice 247`。已把 `rsplit()` 也纳入 direct silent fallback 门禁。
- 为什么改计划：上一刀已补 `encode/decode` 这组转换方法，但 direct silent fallback 仍可借 `return ''.rsplit()`、`return payload.rsplit()` 与 `return tuple(payload.rsplit())` 这类反向分割方法继续绕过；当前 except handler 内的 text rsplit method return 为 `NO_RETURN_TEXT_RSPLIT_METHOD_HITS`，适合立刻把这一层方法调用语法糖补齐。
- 当前最重要的结论：failed remembered session 下的 `cmd retry/status/report/session/sessions` 已锁住，failed 显式上下文下的 `cmd status/report` 已锁住，wrapper `cmd preflight --json`、`cmd workspace --name demo --json`、`cmd workspace --clear --json`、`cmd forget --json`、`cmd seed-failed --json`、`cmd seed-crash --json`、`cmd seed-hibernated --json` 已锁住；原生 `resume --json`、`cmd resume --json`、根入口 `safeclaw.cmd resume --json`、根入口 `safeclaw.cmd seed-hibernated --json`、根入口 `safeclaw.ps1 seed-hibernated --json`、根入口 `safeclaw.ps1 resume --json`、根入口 `safeclaw.ps1 seed-failed --json`、根入口 `safeclaw.ps1 seed-crash --json`、根入口 `safeclaw.ps1 workspace --name readme-root-ps1 --json`、根入口 `safeclaw.ps1 doctor --json`、根入口 `safeclaw.ps1 workspace --clear --json`、根入口 `safeclaw.ps1 forget --json`、根入口 `safeclaw.ps1 preflight --action service-run --json`、根入口 `safeclaw.cmd preflight --action service-run --json`、根入口 `safeclaw.ps1 verify --json`、根入口 `safeclaw.ps1 workspace --json`、根入口 `safeclaw.cmd workspace --json`、根入口 `safeclaw.cmd doctor --json`、根入口 `safeclaw.cmd service-status --limit 5 --json`、根入口 `safeclaw.ps1 service-status --limit 5 --json`、根入口 `safeclaw.cmd preflight --action ai-reason --json`、根入口 `safeclaw.ps1 preflight --action ai-reason --json`、根入口 `safeclaw.cmd preflight --action demo --json`、根入口 `safeclaw.ps1 preflight --action demo --json`、根入口 `safeclaw.ps1 service-run --json`、根入口 `safeclaw.ps1 service-retry --json`、根入口 `safeclaw.ps1 service-recover --json`、根入口 `safeclaw.cmd service-reconcile --json`、根入口 `safeclaw.ps1 service-reconcile --json`、根入口 `safeclaw.cmd service-resume --json` 与根入口 `safeclaw.ps1 service-resume --json` 的成功合同都已锁住；其中 `safeclaw.cmd service-resume --json` 与 `safeclaw.ps1 service-resume --json` 公开的 `resume-target-not-hibernated` / `resume-target-missing` 错误合同也已锁住，`safeclaw.cmd service-run --preflight --preflight-action ai-reason --json`、`safeclaw.ps1 service-run --preflight --preflight-action ai-reason --json`、根入口 `service-retry --preflight --preflight-action ai-reason --json`、根入口 `service-recover --preflight --preflight-action ai-reason --json`、根入口 `demo --preflight --preflight-action ai-reason --json`、根入口 `service-resume --preflight --preflight-action ai-reason --json`、根入口 `service-reconcile --preflight --preflight-action ai-reason --json` 的 `preflight-blocked` 阻断合同也已锁住。接下来需要回到剩余根入口真实差集盘点。
- 计划调整说明：`Slice 172` 已按用户要求把安全抽象层预留位收进 `specs/spi/` 真源目录；若后续继续安全接入，必须在这三组接口位内增量推进；若回到既有 `M1b` 主线，则继续盘点剩余根入口真实差集，不重复已闭环区域。
- budget 侧继续冻结；下一次如果再碰 budget，仍必须先指出真实 source 在哪里。
- 现在的主视图：`doctor` 看环境，`service-status` 看治理快照 + 并发/离线边界，`preflight` 看单动作门禁，组合动作错误 JSON 看浅层顶层错误字段。
- 仓库卫生侧线已补出七层台账门禁：`ledger_index_manifest.py` 负责机读索引，`check_versions.py` 负责 manifest_version / phase 口径，`check_consistency.py` 负责机读索引与文字方案映射不漂移，`check_structure.py` 负责目标路径提前落地护栏，`check_scaffold.py` 负责 legacy 阶段根台账保留，`check_ledger_alignment.py` 负责三份主台账对齐，`check_public_docs.py` 负责公开文档联动；`selfcheck.py` 已显式串起这条链路。
- 计划调整说明：本轮把第七个真实消费点落到 `check_versions.py`，优先锁住 manifest_version 与 phase 语义口径；这比只在 loader 或文档里隐含约束更有长期收益。
- 计划调整说明：本轮先补 `docs/30-方案/20-V4-reference-compliance-rebaseline-record-20260329_030242.md`，因为两份旧合规复核文档已有事实滞后；先校正当前整改基线，再继续第八个真实消费点，长期收益更高。
- 计划调整说明：纠偏快照补完后，先把它接进 `check_public_docs.py`，确保后续继续找第八个真实消费点时，不会再按旧审计的过期说法回退判断。
- 计划调整说明：在继续找第八个真实消费点前，先把 `selfcheck.py` 的 ledger policy chain 显式前置；这样即使 broad contract tests 受历史债影响，ledger 迁移护栏也不会被提前遮住。
- 计划调整说明：顺序前置后，再把这条现行链路写进 `tools/checks/README.md` 并锁进 public docs 合同，避免入口说明落后于真实门禁。
- 计划调整说明：在现行顺序稳定后，再把 `selfcheck.py` 的 ledger policy chain 抽成常量真源；这样后续若继续调整阶段，只需改单处，不再双处手写。
- 计划调整说明：本轮把第八个真实消费点落到 `.github/workflows/contracts.yml`；先让 CI 长期入口显式前置 ledger policy chain，再进 contract tests，长期收益高于继续补同层说明。
- 计划调整说明：第八个消费点落到 CI 后，本轮继续补根 README 与 Operator Playbook 两个维护入口；先让人看到的入口说明也对齐 ledger-first 顺序，再由 public docs 门禁锁住。
- 计划调整说明：本轮把第十个真实消费点落到 `tools/README.md`；先把仓库工具总入口对齐 ledger-first policy，再由 `check_public_docs.py` 锁成长期门禁，长期收益高于继续补次级说明页。
- 计划调整说明：本轮把第十一个真实消费点落到 `docs/IMPLEMENTATION_STRATEGY.md`；先让长期策略文档也显式承认 ledger-first policy，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十二个真实消费点落到 `tests/contracts/README.md`；先让测试长期入口也显式说清 Contract tests 后置于 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补二级索引页。
- 计划调整说明：本轮把第十三个真实消费点落到 `tests/README.md`；先让测试总入口也显式说明 contracts 测试属于 ledger-first policy 之后的阶段，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十四个真实消费点落到 `docs/README.md`；先让公开文档总入口也显式说明现行 ledger-first policy，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十五个真实消费点落到 specs/README.md；先让协议真源总入口也显式说明 ledger-first policy chain 再进入 Contract tests，再由 check_public_docs.py 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十六个真实消费点落到 docs/V1_SCOPE.md；先让当前公开范围入口也显式说明现行 ledger-first policy，再由 check_public_docs.py 锁成公开门禁，长期收益高于继续补边缘说明页。

- 计划调整说明：本轮把第十七个真实消费点落到 `docs/V1_TASK_TRIAGE.md`；先让当前任务分级入口也显式说明现行 ledger-first policy，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十八个真实消费点落到 `docs/DEVLOG.md`；先让长期开发日志入口也显式说明现行 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十九个真实消费点落到 `tools/codegen/README.md`；先让 manifest 生成入口也显式说明现行 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第二十个真实消费点落到 `tools/schema_diff/README.md`；先让 schema diff 自动化入口也显式说明现行 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第二十一个真实消费点落到 `tools/mvp/README.md`；先让本地 MVP 操作入口也显式说明现行 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第二十二个真实消费点落到 `tools/lint/README.md`；先让稳定命名门禁入口也显式说明现行 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：主公已明确 `docs/reference/` 必须升级为硬强制且无需人工提醒，因此暂停继续找第二十三个真实消费点，先把可机器化 reference 规则下沉成 fail-closed 门禁；入口扩展顺延到该硬门禁收口之后。
- 计划调整说明：本轮已把 `docs/reference/01` 中的“无主 TODO”与“空异常处理（第一阶段：pass-only / 空 catch）”落成 `check_reference_redlines.py`，并接进 `selfcheck` 与 CI；下一刀继续扩大 `docs/reference/01` 的硬门禁覆盖，不回到只补说明入口的路径。
- 计划调整说明：本轮继续把异常处理红线推进到第二阶段，新增“多异常 `except` 必须显式绑定 `as error`”门禁，并修平当前唯一命中点；下一刀再考虑继续扩到真正的无上下文降级/吞异常。
- 计划调整说明：本轮把异常处理红线推进到第三阶段，新增“绑定了 `as error` 后，异常上下文必须真正使用，不能只做 `_ = error` 占位赋值”门禁；下一刀继续优先攻“无上下文降级/吞异常”。
- 计划调整说明：本轮继续沿零修复面高复利路径推进，把 `KeyError` 纳入单异常上下文红线；下一刀优先继续盘点 `RuntimeError` / `SyntaxError` 这类低命中且已满足基线的候选，避免过早碰 `ValueError` 的高噪音面。
- 计划调整说明：本轮已把 `RuntimeError` 也纳入单异常上下文红线；下一刀优先评估 `SyntaxError` 是否仍值得继续扩面，若收益不足则转回更高复利的静默降级/真实入口差集主线。
- 计划调整说明：本轮已把 `SyntaxError` 也纳入单异常上下文红线；低噪声单异常候选基本见底，下一刀优先回到“静默降级/吞异常”与真实入口差集这类更高复利主线。
- 计划调整说明：本轮先把 Plan B 所需的本机日用白名单路径锁进 `OPERATOR_PLAYBOOK` 与 public docs gate；后续再继续收静默降级/真实入口差集时，至少有一条可手用路径不会随文档漂移。
- 计划调整说明：本轮继续把根 `README.md` 与 `OPERATOR_PLAYBOOK` 做成双向互链；下一刀优先考虑让 `tools/mvp/README.md` 也对白名单路径显式让位，减少三份入口文档的语义漂移。
- 计划调整说明：本轮改优先级，先修 `service-status` 的心跳误报，把“历史已完成任务”与“真实故障”解耦；接下来再继续排查治理视图中其他历史/实时语义混淆点。
- 计划调整说明：本轮先把“高风险 `OSError/json.JSONDecodeError` 直接 `return None/False`”收成第四阶段静默降级红线，并修平 `mvp_state_guard.py` 的唯一命中点；下一刀再决定继续扩大降级形态，或切去“单函数 / 单类体量红线”。
- 计划调整说明：本轮顺手清掉了 `specs/spi/*` 缺少 `version` 的历史总阻塞，并把同批 `$schema/$id/title` 与 `generated/` 索引一并补齐；`selfcheck` 已恢复全绿，下一刀回到 reference 红线主线推进。
- 计划调整说明：本轮继续沿异常红线主线推进，新增 `json.JSONDecodeError` 必须绑定并保留上下文的门禁，并修平当前 3 个真实命中点；下一刀优先继续扩大单异常上下文红线，或切去“单函数 / 单类体量红线”。
- 计划调整说明：本轮继续沿“当前上下文真源对齐”主线推进，把 `use` 切换任务时的 output 也强制跟随目标任务 `target_scope`；下一刀优先继续排查是否还有“任务身份已切、但上下文字段仍留旧值”的残留点。
- 计划调整说明：本轮先把 `Slice 185` 遗留的 broad smoke 债务收平，不继续让 `use.output_source` 的旧合同长期漂移；下一刀优先处理已复现的 `owner_id` 残留问题。
- 计划调整说明：本轮继续沿“当前上下文真源对齐”主线推进，把 `use` 切任务时残留的 `owner_id` 也收回目标任务真源；下一刀优先处理 Windows 下 stale `.wrapper-check.lock` 的自恢复问题。
- 计划调整说明：本轮不再继续堆新的业务语义切片，先把验证基础设施上的 Windows stale-lock 假阻塞收平；下一刀优先考虑补 `acquire_mvp_state_lock()` 的端到端 stale-lock 合同。
- 计划调整说明：本轮继续把验证基础设施收成可交账稳态，把 stale-lock 自恢复从 helper 级补到端到端合同；下一刀优先评估 `LOCK_ENV` 嵌套复用语义是否还需单独锁住。
- 计划调整说明：本轮继续把验证基础设施收成可复用稳态，把 `LOCK_ENV` 嵌套复用语义补成端到端合同；下一刀优先回到 `docs/reference/01` 的高复利 fail-closed 门禁扩面。
- 计划调整说明：本轮已回到 `docs/reference/01` 主线，先把裸 `except:` / broad `Exception` 的上下文要求补成明确合同；下一刀优先继续扩“静默降级异常形态”或更易落地的复杂度红线。
- 计划调整说明：本轮继续沿单异常上下文主线扩面，把 `SystemError` 也收成 fail-closed 门禁并修平唯一真实命中；下一刀优先继续挑命中更少的静默降级异常形态，不贸然碰高爆炸面的 `ValueError`。
- 计划调整说明：本轮继续沿单异常上下文主线扩面，把 `subprocess.TimeoutExpired` 也收成 fail-closed 门禁；下一刀仍优先挑低命中静默降级异常形态，不贸然碰 `ValueError`。
- 计划调整说明：本轮切到静默降级主线，把 `SystemError` / `subprocess.TimeoutExpired` 也纳入 direct fallback 门禁；下一刀仍优先挑低命中静默降级异常，不贸然碰 `ValueError`。
- 计划调整说明：本轮把高风险静默降级异常名单统一成单一真源；下一刀仍优先挑低命中异常或同类可统一真源的规则，不贸然碰 `ValueError`。
- 计划调整说明：本轮继续把高风险异常规则收成单一维护面；下一刀仍优先挑还能并入同类真源、且不需碰高爆炸面运行时代码的规则。
- 计划调整说明：本轮把 broad except 家族补成统一语义：BaseException 也按 broad except 处理，且 broad/裸 except direct fallback 已 fail-closed；下一刀仍优先挑低命中、可并入同类真源的小切片，不贸然碰高爆炸面的 ValueError。
- 计划调整说明：本轮继续沿 broad exception family 主线，把 tuple broad handler 的 direct fallback 漏口也收成 fail-closed；下一刀仍优先找 broad / multi-exception 家族的剩余小边角，不贸然碰高爆炸面的 ValueError。
- 计划调整说明：本轮继续沿 broad exception family 主线，把 tuple broad handler 的缺少上下文提示也统一到 broad except 语义；下一刀仍优先找 broad / multi-exception 家族的剩余小边角，不贸然碰高爆炸面的 ValueError。
- 计划调整说明：本轮把异常门禁提示文案收成单一真源；下一刀仍优先找 broad / multi-exception 家族剩余的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把 broad family 的 handler 识别逻辑收成 helper 真源；下一刀仍优先找 broad / multi-exception 家族最后的重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把 handler 的 caught_types 解析继续压成单一真源；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把 caught_types helper 从“已有真源”推进到“关键消费点全面复用”；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把高风险异常名单从“值相等的两套集合”继续压成“同一真源对象”；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把 handler 识别链从“多个 helper + 多处 if 分叉”继续压成“单画像真源 + 多消费点复用”；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把高风险异常交叉与有序名单也并入 handler profile；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把“画像 → 门禁文案”也并入 handler profile；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把最后两段门禁布尔判定也并入 handler profile；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把“遍历 handler + 构建画像”也收成单一入口；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把四处重复的 Python 解析骨架也收成单一真源；下一刀仍优先找 broad / multi-exception 家族最后的小重复点，若基本收平，再谨慎评估更窄的 ValueError 子集。
- 计划调整说明：本轮把三条顶层 Python 文件扫描骨架也收成单一真源；下一刀仍可评估 TODO / empty-exception / PowerShell 扫描器是否值得一并收口，或回到 broad / multi-exception 家族最后的小重复点。
- 计划调整说明：本轮把顶层 reference 文件文本扫描骨架也收成单一真源；TODO / empty-exception / Python collector 已共用同一入口，下一刀优先回到 `docs/reference/01` 的异常红线扩面。
- 计划调整说明：本轮把“静默降级名单”与“上下文强绑定名单”拆成双真源，并先把 `ValueError -> None/False` 收进 fail-closed；下一刀优先继续评估空字符串 / 空容器这类降级形态。
- 计划调整说明：本轮把 `TypeError` 也纳入 direct 静默降级门禁；下一刀优先继续盘空字符串 / 空容器这类零命中降级形态，能零债补门禁就先补。
- 计划调整说明：本轮已把空字符串 / 空容器 direct silent fallback 收成统一门禁；下一刀优先继续沿 `docs/reference/01` 主线找同类零命中、可并入现有真源的降级形态，不扩散到高爆炸面业务改造。
- 计划调整说明：本轮已把 `set()` / `frozenset()` 也收进“空容器”门禁；下一刀优先继续找还能零债并入当前 silent fallback 真源的相邻表达，避免“文案已覆盖、实现未覆盖”的语义裂缝。
- 计划调整说明：本轮已把整组无参空值构造调用收进 silent fallback 真源；下一刀优先继续找 silent fallback 里仍可能存在的“语义已写进文案、实现未完全对齐”的零命中表达，不扩散到高爆炸面旧代码。
- 计划调整说明：本轮已把隐式 bare return 收进 silent fallback 真源，并把仓内 2 个真实命中清零；下一刀优先继续找 still-zero-hit 的 silent fallback 相邻形态，不回头重复已闭环区域。
- 计划调整说明：本轮已把 `bool()` 也收进 silent fallback constructor 真源；下一刀优先继续找这条 silent fallback 家族里仍可能存在的零命中调用/表达漏口，避免语义已闭环却仍残留实现别名。
- 计划调整说明：本轮已把“两步静默降级”也收进 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族里最后的零命中绕行写法，避免规则只拦最直白写法。
- 计划调整说明：本轮已把 `AnnAssign` 两步静默降级也收进 direct silent fallback 真源；下一刀优先继续盘 silent fallback 家族最后的零命中绕行表达，避免只靠语法糖就绕过门禁。
## 三、下一候选（按优先级排序）
1. 下一刀优先继续扩大 `docs/reference/01` 的硬门禁覆盖
   - 目标：优先把异常处理继续从“高风险 I/O/JSON 不能直接静默降级”扩大到更多单异常降级形态，或把“单函数 / 单类体量红线”转成可执行检查，不重复停留在人工提醒层。
   - 原因：`无主 TODO`、`pass-only catch`、多异常 `except` 绑定上下文、绑定后必须真正使用上下文，以及高风险 I/O/JSON 直接 `return None/False` 都已收口，当前最高复利不是再补说明入口，而是继续把代码红线沉到 fail-closed 门禁。
   - 前提：仍以 `docs/30-方案/20-V4-reference-compliance-rebaseline-record-20260329_030242.md` 和 `docs/30-方案/02-V4-目录锁定清单.md` 作为当前 reference 落地边界，避免把结构整改与业务推进混做一刀。
2. 第二十三个最小真实消费点恢复候选
   - 目标：在 reference 硬门禁稳定后，再回到尚未消费 manifest 的真实入口继续接入，不重复包裹已有 versions、consistency、structure、scaffold、public docs、selfcheck、CI contracts workflow、tools 根入口、长期策略文档、contracts 测试入口、tests 总入口、docs 总入口、specs 真源总入口、当前公开范围入口、当前任务分级入口、开发日志入口、codegen 生成入口、schema diff 入口、MVP 操作入口与 lint 命名门禁入口。
3. budget 侧继续冻结
   - 前提：除非先出现真实 runtime/source，例如 mmap 路径、reservation counters、reconcile/drift 统计或可复用配置骨架；否则不再重复开 budget 面板切片。
## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 伪造 budget/operator 静态占位字段
- 多 Worker 并发与 scope-level mutex 大改
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 台账写法要求：`MVP_PROGRESS.md`、`PUSH_LOG.md` 尽量使用中文、短句、小学生能懂；先写“做了什么”，再写“有什么用”。
- 验证顺序固定：`py_compile -> check_versions.py -> check_consistency.py -> check_structure.py -> check_scaffold.py -> check_ledger_alignment.py -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 蓝图基线：`01_文档/03_开发蓝图.md`
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 台账门禁：`tools/checks/check_versions.py`、`tools/checks/check_consistency.py`、`tools/checks/check_ledger_alignment.py`、`tools/checks/check_structure.py`、`tools/checks/check_scaffold.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`
## 七、下一刀完成判据
- 先把 `docs/reference/01` 的下一组可机器化红线补成 fail-closed 校验，并确认旧代码基线不过度误伤。
- 当前 `无主 TODO`、`空异常处理`、异常上下文绑定/使用、高风险静默降级最小形态，以及 `specs/spi/*` 的 version/contract metadata 历史阻塞都已收口；若继续扩异常处理，需明确下一批降级形态与允许例外。
- 若 reference 硬门禁阶段性稳定，再回到第二十三个最小真实消费点；届时需先说明为什么不是在重复包一层已有检查器。
- 若下一刀重新回到 budget 侧，需先说明真实数据源来自哪里，以及为什么这次不再是伪合同。
- 相关文本 / JSON / smoke / docs 不回归。
## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀候选发生变化，必须先在本文件补一行“为什么改计划”；若未验真，不提前写死后续 Slice 编号。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。
- 计划调整说明：本轮把 `b''`、`bytes()`、`bytearray()` 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中别名形态，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把空 `f-string` 语法别名也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中语法糖或别名绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把静态单参空值构造 alias 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中包装层或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把静态 `IfExp` / `BoolOp` 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中包装层或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把静态 `Compare` / `UnaryOp(not)` 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中包装层或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把静态 `BinOp` 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中包装层或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- ???????????? `Assign` / `AnnAssign` assignment chain ??? direct silent fallback ??????????? silent fallback ????????? helper ????????????????????????????????
- 计划调整说明：本轮把多步 `Assign` / `AnnAssign` assignment chain 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 包装层或更深别名绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把多目标 `Assign` assignment 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把单参 return call constructor 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 nested return call constructor 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `return constructor(ifexp)` 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `return constructor(boolop)` 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `return constructor(unary/compare)` 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `return constructor(binop)` 包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `str(object=...)` / `bytes(source=...)` / `bytearray(source=...)` 关键字构造包装也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `Subscript` 切片语法糖也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装或 helper 绕行，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 unpack literal 语法糖也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中更深 alias 包装、helper 绕行或更边角的表达式语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `NamedExpr`（海象表达式）也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、构造包装或更边角的表达式语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `JoinedStr` / `f-string` 表达式也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的表达式语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `copy()` 零参方法也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `strip/lower/rstrip` 这组 zero-arg text method 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `split()/splitlines()` 这组 zero-arg text split method 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `isalnum/isalpha/isdecimal/isdigit/isidentifier/islower/isnumeric/isspace/istitle/isupper` 这组 zero-arg text predicate method 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `capitalize/swapcase/title/expandtabs` 这组 zero-arg text transform method 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `encode/decode` 这组 zero-arg text conversion method 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。
- 计划调整说明：本轮把 `rsplit()` 也纳入 direct silent fallback 真源；下一刀优先继续找 silent fallback 家族是否还剩零命中 helper 绕行、constructor 包装或更边角的 zero-arg method / expression 语法糖，若这一族见底，再切回另一条同级高复利红线。

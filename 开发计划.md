# 开发计划

最后更新时间：2026-03-26 02:31:54 +0800
当前基线提交：`9d53f30` （本轮完成后将更新为新 HEAD）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环

## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 25 刀，最新已落地 `Slice 25` 离线降级错误码闭环。
- `target/mvp` 是共享验证状态目录；`check_tooling_smoke.py` 与 `check_mvp_operator_flow.py` 必须线性顺跑，不能并发。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`。

## 二、当前状态
- 最新完成：`preflight --action ai-reason` 已可稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，同时暴露 `requires_model=true`、`requires_sidecar=true`、`error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- 现在的主视图：`doctor` 看运行环境，`service-status` 看治理快照，`preflight` 看离线可用性和权限门禁。
- 本轮新增的 `ai-reason` 只是 preflight-only 占位动作，不是真实执行入口。

## 三、下一候选（按优先级排序）
1. `Slice 26`：`service-status` 顶层离线阻断提示
   - 目标：把 provider 不可用的离线事实折成 operator 可直读的摘要。
   - 前提：仅做顶层可见化，不接真 model/provider。
2. `Slice 27`：offline / provider 差错码在组合动作中的稳定传递
   - 目标：如果后面出现 AI 组合动作，保证 `error.details.preflight` 还能带出这个错误码。

## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 多 Worker 并发与 scope-level mutex
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 验证顺序固定：`py_compile -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`

## 七、执行完成判据
- `preflight --action ai-reason` 文本输出出现稳定阻断原因。
- `preflight --action ai-reason --json` 返回稳定字段和错误码。
- 所有既有 `service-run/service-status` 门禁不回归。
- `check_public_docs.py`、`check_tooling_smoke.py`、`check_mvp_operator_flow.py`、`selfcheck.py` 全绿。

## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀不是 `Slice 26`，必须先在本文件补一行“为什么改计划”。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。

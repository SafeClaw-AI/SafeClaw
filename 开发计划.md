# 开发计划

最后更新时间：2026-03-26 04:29:03 +0800
当前基线提交：`8b4ee0d`（本轮起点）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环

## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 29 刀，最新已落地 `Slice 29`：组合动作 preflight 顶层 `error.code`。
- `preflight --action ai-reason` 已稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，并暴露 `requires_model=true` / `requires_sidecar=true` / `error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- `service-status` 已同步回显 top-level `runtime_profile` / `model_provider` / `sidecar` / `offline_gate`。
- `demo` / `service-run` / `service-retry` / `service-recover` / `service-reconcile` 现支持 `--preflight-action <name>`，可在组合动作里复用不同 preflight 合同。
- 当组合动作因 preflight 被阻断时，`error.details.preflight` 会稳定保留完整门禁载荷；同时会在顶层 `error.code` 镜像 `preflight-blocked`，并镜像 `preflight_requested_action` / `preflight_reason` / `preflight_summary` / 可选 `preflight_error_code` 到 `error.details` 顶层。
- 如使用 `--preflight-action ai-reason`，则 `error.details.preflight.error_code` 与 `error.details.preflight_error_code` 都稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`。
- `target/mvp` 是共享验证状态目录；`check_tooling_smoke.py` 与 `check_mvp_operator_flow.py` 必须线性顺跑，不能并发。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。

## 二、当前状态
- 最新完成：组合动作的 preflight 阻断错误现会在顶层 `error.code` 稳定回显 `preflight-blocked`，脚本不必再深入 `error.details.code` 才能拿到组合级错误码。
- 当前最重要的已验证路径：`service-run --preflight --preflight-action ai-reason --json` 与 `demo --preflight --preflight-action ai-reason --json` 都会稳定返回顶层 `error.code=preflight-blocked`，以及 `error.details.preflight_requested_action=ai-reason`、`preflight_reason=ERR_AI_PROVIDER_UNAVAILABLE`、`preflight_error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- 权限阻断路径也已同步镜像：如 `service-run --enforce-permission --json`，会稳定返回顶层 `error.code=preflight-blocked`、`preflight_requested_action=service-run` 与 `preflight_reason=write_scope_requires_confirmation`，且不会伪造 `preflight_error_code`。
- 现在的主视图：`doctor` 看运行环境，`service-status` 看治理快照 + 运行边界 + 离线阻断提示，`preflight` 看单动作门禁细节，组合动作错误 JSON 看顶层 `error.code` + preflight 阻断细节与快捷字段。
- 当前 `ai-reason` 仍是 preflight-only 占位动作，不是真实执行入口。

## 三、下一候选（按优先级排序）
1. `Slice 30`：组合动作阻断错误的顶层 `error.reason`
   - 目标：让 `preflight-blocked` 这类组合动作阻断，在已有顶层 `error.code` 之外，也能直接从顶层 `error.reason` 读取既有 `preflight_reason`，进一步减少脚本读取 `error.details` 的层级。
   - 前提：仅镜像现有 `preflight_reason`，不扩展新语义，不改 Rust。
2. `Slice 30` 收稳后再定下一刀
   - 原因：当前 provider-unavailable 合同已经覆盖 `preflight -> service-status -> combo error.details.preflight -> combo shallow shortcuts -> combo error.code` 五个视角，下一步仍优先继续降低脚本接入摩擦，而不是扩展新功能面。

## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 多 Worker 并发与 scope-level mutex
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 验证顺序固定：`py_compile -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`

## 七、下一刀完成判据
- 组合动作阻断时，脚本可直接从顶层 `error.reason` 读取既有 preflight 原因，而不必进入 `error.details.preflight_reason`。
- 相关组合动作文本 / JSON / smoke 不回归。
- `check_public_docs.py`、`check_tooling_smoke.py`、`check_mvp_operator_flow.py`、`selfcheck.py` 全绿。

## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀不是 `Slice 30`，必须先在本文件补一行“为什么改计划”。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。


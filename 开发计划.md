# 开发计划

最后更新时间：2026-03-26 08:09:28 +0800
当前基线提交：`09f0dd0`（Slice 42 测试护栏基线）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环

## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 42 刀；`Slice 40` 做完 budget/operator 盘点，`Slice 41` 锁住了并发 happy-path 基线，`Slice 42` 又把 `executed_assumed -> service-reconcile` 这条自隔离边缘路径锁进了 operator-flow 护栏。
- `preflight --action ai-reason` 已稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，并暴露 `requires_model=true` / `requires_sidecar=true` / `error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- `service-status` 现在已经能稳定回显 `runtime_profile` / `model_provider` / `sidecar` / `offline_gate` / `heartbeat` / `coordination` / `recent_tasks`。
- 经 `Slice 40` 盘点，蓝图里的财务熔断 / 预算状态目前还只在蓝图与 specs 里；现有 wrapper/operator 视图还没有真实 `budget` 数据源可直接复用。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。

## 二、当前状态
- 最新完成：`Slice 42`。`check_mvp_operator_flow.py` 现在会先造出 `executed_assumed`，再检查 `service-status` 在 self-quarantine 现场是否稳定回显 `quarantined` / `self_executed_assumed_scope_quarantine` / `reconcile_self_before_scope_write`、`next_task_id`、`next_command` 与 `reconcile_commands`，最后再检查 `service-reconcile` 组合动作是否能把这条边缘路径收回 `clear` 基线。
- 当前最重要的结论：并发边缘场景不是完全没护栏；`self quarantine -> reconcile` 这条最关键的 operator 收口路径，现在已经进了 operator-flow。
- 预算视图仍先不做；因为现在还没有真实 budget runtime/source，硬补只会变成假面板。
- 现在的主视图：`doctor` 看环境，`service-status` 看治理快照 + 并发/离线边界，`preflight` 看单动作门禁，组合动作错误 JSON 看浅层顶层错误字段。

## 三、下一候选（按优先级排序）
1. `Slice 43`：active-lease stalled operator-flow guardrail
   - 目标：把 `stalled` / `active_lease_without_recent_heartbeat` 这条等待/检查路径补进 operator-flow，确认 `wait_ms`、`next_blocker=active_lease`、`coordination=stalled` 不会静默漂移。
   - 原因：这是并发边缘场景里第二高频、且最容易误导 operator 的一条路径；同时已有 smoke 造景可复用，返工成本低。
2. `Slice 44` 候选：peer quarantine / contended 盘点
   - 前提：若 `stalled` 护栏补完，再看 `same_scope_peer_active` 与 `peer_executed_assumed_scope_quarantine` 是否还缺 operator-flow 护栏。
3. 预算侧后续前置条件确认
   - 前提：先出现真实 runtime/source，例如 budget mmap 路径、reservation counters、reconcile/drift 统计或可复用配置骨架；届时再决定是否做 `doctor` / `service-status` 预算快照。

## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 伪造 budget/operator 静态占位字段
- 多 Worker 并发与 scope-level mutex 大改
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 台账写法要求：`MVP_PROGRESS.md`、`PUSH_LOG.md` 尽量使用中文、短句、小学生能懂；先写“做了什么”，再写“有什么用”。
- 验证顺序固定：`py_compile -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 蓝图基线：`01_文档/03_开发蓝图.md`
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`

## 七、下一刀完成判据
- 完成一轮 `active_lease -> stalled` operator-flow 盘点，并明确还缺不缺最小护栏。
- 若下一刀重新回到预算侧，需先说明真实数据源来自哪里，以及为什么这次不再是伪合同。
- 相关文本 / JSON / smoke / docs 不回归。

## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀不是 `Slice 43`，必须先在本文件补一行“为什么改计划”。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。

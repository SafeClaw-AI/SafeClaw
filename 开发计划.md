# 开发计划

最后更新时间：2026-03-29 03:55:18 +0800
当前基线提交：`f03bc16`（ledger version semantics 代码基线）
当前主线：`M1b 生存层补完`
当前模式：小步快跑、每轮形成可验证闭环

## 一、当前已知稳定事实
- `M1a` 已收口，根入口 `workspace -> doctor -> service-run/retry/recover -> verify` 已进自动门禁。
- `M1b` 已完成前 172 刀；`Slice 40` 做完 budget/operator 盘点，`Slice 41` 锁住了并发 happy-path 基线，`Slice 42` 把 `executed_assumed -> service-reconcile` 自隔离收口路径锁进了 operator-flow，`Slice 43` 把 `active lease -> stalled` 等待路径补进了 operator-flow，`Slice 44` 把 peer quarantine / contended 两条 peer 边缘路径锁进了 operator-flow，`Slice 45` 确认 budget 仍无真实 runtime/source 并补上 no-fake-budget 护栏，`Slice 46` 把 hibernated 现场补成了显式可读提示，`Slice 47` 把 hibernated 现场继续补成了真实 `resume` / `service-resume` 入口，`Slice 48` 把这条冬眠收口链继续升成了 operator-flow 真门禁，`Slice 49` 补齐接错现场失败合同，`Slice 50` 锁住 `service-resume` 缺上下文合同，`Slice 51` 锁住 `service-reconcile` 缺上下文合同，`Slice 52` 锁住原生 `reconcile` 缺上下文合同，`Slice 53` 锁住原生 `resume` 缺上下文合同，`Slice 54` 锁住 `cmd resume` 缺上下文合同，`Slice 55` 锁住 `cmd reconcile` 缺上下文合同，`Slice 56` 锁住 `ps1 resume` 缺上下文合同，`Slice 57` 锁住 `ps1 reconcile` 缺上下文合同，`Slice 58` 锁住 `ps1 report` invalid-json remembered-session 合同，`Slice 59` 锁住 `ps1 retry` invalid-json remembered-session 合同，`Slice 60` 锁住 `ps1 session` invalid-json 合同，`Slice 61` 锁住 `ps1 sessions` invalid-limit 合同，`Slice 62` 锁住 `cmd doctor` missing-value-after-db 合同，`Slice 63` 锁住 `cmd verify` invalid-json 合同，`Slice 64` 锁住 `ps1 verify` invalid-json 合同，`Slice 65` 再把 `cmd recover` invalid-json remembered-session 合同锁进 smoke，`Slice 66` 再把 `ps1 demo` fail-json remembered-session 合同锁进 smoke，`Slice 67` 再把 `ps1 recover-demo` fail-json remembered-session 合同锁进 smoke，`Slice 68` 再把 `ps1 retry-demo` fail-json remembered-session 合同锁进 smoke，`Slice 69` 再把 `ps1 status` invalid-json remembered-session 合同锁进 smoke，`Slice 70` 再把 `cmd status` missing-value-after-db remembered-session 合同锁进 smoke，`Slice 71` 再把 `cmd report` missing-task-context 合同锁进 smoke，`Slice 72` 再把 `cmd retry` missing-task-context 合同锁进 smoke，`Slice 73` 再把 `ps1 recover` missing-task-context 合同锁进 smoke，`Slice 74` 再把 `cmd service-run` invalid-limit 合同锁进 smoke，`Slice 75` 再把 `ps1 service-run` invalid-limit 合同锁进 smoke，`Slice 76` 再把 `cmd service-retry` invalid-limit 合同锁进 smoke，`Slice 77` 再把 `ps1 service-retry` invalid-limit 合同锁进 smoke，`Slice 78` 再把 `cmd service-recover` invalid-limit 合同锁进 smoke，`Slice 79` 再把 `ps1 service-recover` invalid-limit 合同锁进 smoke，`Slice 80` 再把 `cmd service-status` invalid-limit 合同锁进 smoke，`Slice 81` 再把 `ps1 service-status` invalid-limit 合同锁进 smoke，`Slice 82` 再把 `ps1 service-status` 成功 JSON 合同锁进 smoke，`Slice 83` 再把 `ps1 service-run` 成功 JSON 合同锁进 smoke，`Slice 84` 再把 `ps1 service-run --report` 成功 JSON 合同锁进 smoke，`Slice 85` 再把 `ps1 service-retry` 成功 JSON 合同锁进 smoke，`Slice 86` 再把 `ps1 service-retry --report` 成功 JSON 合同锁进 smoke，`Slice 87` 再把 `ps1 service-recover` 成功 JSON 合同锁进 smoke，`Slice 88` 再把 `ps1 service-recover --report` 成功 JSON 合同锁进 smoke，`Slice 89` 再把 `ps1 service-resume` 成功 JSON 合同锁进 smoke，`Slice 90` 再把 `ps1 service-resume --report` 成功 JSON 合同锁进 smoke，`Slice 91` 再把 `ps1 service-reconcile` 成功 JSON 合同锁进 smoke，`Slice 92` 再把 `ps1 service-reconcile --report` 成功 JSON 合同锁进 smoke，`Slice 93` 再把 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 94` 再把 `ps1 retry` 成功 JSON 合同锁进 smoke，`Slice 95` 再把 `ps1 recover` 成功 JSON 合同锁进 smoke，`Slice 96` 再把 `ps1 status` 显式成功 JSON 合同锁进 smoke，`Slice 97` 再把 `ps1 report` 显式成功 JSON 合同锁进 smoke，`Slice 98` 再把 crash 现场 `ps1 status` 显式成功 JSON 合同锁进 smoke，`Slice 99` 再把 crash 现场 `ps1 report` 显式成功 JSON 合同锁进 smoke，`Slice 100` 再把 crash 连续性下的 `ps1 session` 成功 JSON 合同锁进 smoke，`Slice 101` 再把 crash 连续性下的 `ps1 sessions` 成功 JSON 合同锁进 smoke，`Slice 102` 再把 crash 会话连续性下无参 `ps1 status` 成功 JSON 合同锁进 smoke，`Slice 103` 再把 crash 会话连续性下无参 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 104` 再把 crash 会话连续性下无参 `ps1 recover` 成功 JSON 合同锁进 smoke，`Slice 105` 再把 failed 会话连续性下无参 `ps1 retry` 成功 JSON 合同锁进 smoke，`Slice 106` 再把 failed 会话连续性下无参 `ps1 status` 成功 JSON 合同锁进 smoke，`Slice 107` 再把 failed 会话连续性下无参 `ps1 report` 成功 JSON 合同锁进 smoke，`Slice 108` 再把 failed 会话连续性下无参 `ps1 session` 成功 JSON 合同锁进 smoke，`Slice 109` 再把 failed 会话连续性下无参 `ps1 sessions` 成功 JSON 合同锁进 smoke，`Slice 110` 再把 failed 显式上下文 `ps1 status --db ... --task-id ... --json` 成功 JSON 合同锁进 smoke，`Slice 111` 再把 failed 显式上下文 `ps1 report --db ... --task-id ... --json` 成功 JSON 合同锁进 smoke，`Slice 112` 再把无参 `cmd status --json` 成功 JSON 合同锁进 smoke。，`Slice 113` 再把无参 `cmd session --json` 成功 JSON 合同锁进 smoke。，`Slice 114` 再把无参 `cmd sessions --json` 成功 JSON 合同锁进 smoke。，`Slice 115` 再把无参 `cmd retry --json` 成功 JSON 合同锁进 smoke。，`Slice 116` 再把无参 `cmd recover --json` 成功 JSON 合同锁进 smoke。，`Slice 117` 再把 failed 显式上下文 `cmd status --json` 成功 JSON 合同锁进 smoke，`Slice 118` 再把 failed 显式上下文 `cmd report --json` 成功 JSON 合同锁进 smoke，`Slice 119` 再把 failed 会话连续性下无参 `cmd status --json` 成功 JSON 合同锁进 smoke，`Slice 120` 再把 failed 会话连续性下无参 `cmd report --json` 成功 JSON 合同锁进 smoke，`Slice 121` 再把 failed 会话连续性下无参 `cmd session --json` 成功 JSON 合同锁进 smoke，`Slice 122` 再把 failed 会话连续性下无参 `cmd sessions --json` 成功 JSON 合同锁进 smoke，`Slice 123` 再把 `cmd preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 124` 再把 `cmd workspace --name demo --json` 成功 JSON 合同锁进 smoke，`Slice 125` 再把 `cmd seed-failed --json` 成功 JSON 合同锁进 smoke，`Slice 126` 再把 `cmd seed-crash --json` 成功 JSON 合同锁进 smoke，`Slice 127` 再把 `cmd seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 128` 再把 `cmd workspace --clear --json` 成功 JSON 合同锁进 smoke，`Slice 129` 再把 `cmd forget --json` 成功 JSON 合同锁进 smoke，`Slice 130` 再把原生 `resume --json` 成功 JSON 合同锁进 smoke，`Slice 131` 再把 `cmd resume --json` 成功 JSON 合同锁进 smoke，`Slice 132` 再把根入口 `safeclaw.cmd resume --json` 成功 JSON 合同锁进 smoke，`Slice 133` 再把根入口 `safeclaw.cmd seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 134` 再把根入口 `safeclaw.ps1 seed-hibernated --json` 成功 JSON 合同锁进 smoke，`Slice 135` 再把根入口 `safeclaw.ps1 resume --json` 成功 JSON 合同锁进 smoke，`Slice 136` 再把根入口 `safeclaw.ps1 seed-failed --json` 成功 JSON 合同锁进 smoke，`Slice 137` 再把根入口 `safeclaw.ps1 seed-crash --json` 成功 JSON 合同锁进 smoke，`Slice 138` 再把根入口 `safeclaw.ps1 workspace --name readme-root-ps1 --json` 成功 JSON 合同锁进 smoke，`Slice 139` 再把根入口 `safeclaw.ps1 doctor --json` 成功 JSON 合同锁进 smoke，`Slice 140` 再把根入口 `safeclaw.ps1 workspace --clear --json` 成功 JSON 合同锁进 smoke，`Slice 141` 再把根入口 `safeclaw.ps1 forget --json` 成功 JSON 合同锁进 smoke，`Slice 142` 再把根入口 `safeclaw.ps1 preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 143` 再把根入口 `safeclaw.cmd preflight --action service-run --json` 成功 JSON 合同锁进 smoke，`Slice 144` 再把根入口 `safeclaw.ps1 verify --json` 成功 JSON 合同锁进 smoke，`Slice 145` 再把根入口 `safeclaw.ps1 workspace --json` 默认态 JSON 合同锁进 smoke，`Slice 146` 再把根入口 `safeclaw.cmd workspace --json` 默认态 JSON 合同锁进 smoke，`Slice 147` 再把根入口 `safeclaw.cmd doctor --json` 默认态 JSON 合同锁进 smoke，`Slice 148` 再把根入口 `safeclaw.cmd service-status --limit 5 --json` 默认态 JSON 快照合同锁进 smoke，`Slice 149` 再把根入口 `safeclaw.ps1 service-status --limit 5 --json` 默认态 JSON 快照合同锁进 smoke，`Slice 150` 再把根入口 `safeclaw.cmd preflight --action ai-reason --json` 的 provider-unavailable deny JSON 合同锁进 smoke，`Slice 151` 再把根入口 `safeclaw.ps1 preflight --action ai-reason --json` 的 provider-unavailable deny JSON 合同锁进 smoke，`Slice 152` 再把根入口 `safeclaw.cmd preflight --action demo --json` 的 allow JSON 合同锁进 smoke，`Slice 153` 再把根入口 `safeclaw.ps1 preflight --action demo --json` 的 allow JSON 合同锁进 smoke，`Slice 154` 再把根入口 `safeclaw.ps1 service-run --json` 的成功 JSON 合同锁进 smoke，`Slice 155` 再把根入口 `safeclaw.ps1 service-retry --json` 的成功 JSON 合同锁进 smoke，`Slice 156` 再把根入口 `safeclaw.ps1 service-recover --json` 的成功 JSON 合同锁进 smoke，`Slice 157` 再把根入口 `safeclaw.cmd service-reconcile --json` 的成功 JSON 合同锁进 smoke，`Slice 158` 再把根入口 `safeclaw.cmd service-resume --json` 的成功 JSON 合同锁进 smoke，`Slice 159` 再把根入口 `safeclaw.ps1 service-resume --json` 的成功 JSON 合同锁进 smoke，`Slice 160` 再把根入口 `safeclaw.ps1 service-reconcile --json` 的成功 JSON 合同锁进 smoke。
- `preflight --action ai-reason` 已稳定返回 `ERR_AI_PROVIDER_UNAVAILABLE`，并暴露 `requires_model=true` / `requires_sidecar=true` / `error_code=ERR_AI_PROVIDER_UNAVAILABLE`。
- `service-status` 现在已经能稳定回显 `runtime_profile` / `model_provider` / `sidecar` / `offline_gate` / `heartbeat` / `coordination` / `recent_tasks`。
- 经 `Slice 40` + `Slice 45` 两次复查，蓝图里的财务熔断 / 预算状态目前还只在蓝图、specs 和错误码里；现有 wrapper/operator 视图仍没有真实 `budget` 数据源可直接复用。
- 当前可靠 Python 路径：`C:\Users\tianduan999\anaconda3\python.exe`。
- 每轮完成后必须同步：`MVP_PROGRESS.md`、`PUSH_LOG.md`、`开发计划.md`。

## 二、当前状态
- 最新完成：`Slice 172`。已在 `specs/spi/` 补上 `keystore`、`boot-integrity`、`storage-encryption` 三组安全抽象层接口预留位，并为每组定义 `interface.md`、软件基线 JSON 与硬件占位 JSON；这刀只冻结接口边界、输入输出与未来模块，不落任何运行时代码。
- 为什么改计划：`service-resume` 这组组合 preflight 合同收口后继续回到 README 根入口差集现场，确认下一组真正还没被独立 smoke 覆盖的是 `service-reconcile --preflight --preflight-action ai-reason --json`；这一轮沿用现成 executed-assumed 基座，且 cmd/ps1 都属同类公开合同，合并为一刀一起收口。
- 当前最重要的结论：failed remembered session 下的 `cmd retry/status/report/session/sessions` 已锁住，failed 显式上下文下的 `cmd status/report` 已锁住，wrapper `cmd preflight --json`、`cmd workspace --name demo --json`、`cmd workspace --clear --json`、`cmd forget --json`、`cmd seed-failed --json`、`cmd seed-crash --json`、`cmd seed-hibernated --json` 已锁住；原生 `resume --json`、`cmd resume --json`、根入口 `safeclaw.cmd resume --json`、根入口 `safeclaw.cmd seed-hibernated --json`、根入口 `safeclaw.ps1 seed-hibernated --json`、根入口 `safeclaw.ps1 resume --json`、根入口 `safeclaw.ps1 seed-failed --json`、根入口 `safeclaw.ps1 seed-crash --json`、根入口 `safeclaw.ps1 workspace --name readme-root-ps1 --json`、根入口 `safeclaw.ps1 doctor --json`、根入口 `safeclaw.ps1 workspace --clear --json`、根入口 `safeclaw.ps1 forget --json`、根入口 `safeclaw.ps1 preflight --action service-run --json`、根入口 `safeclaw.cmd preflight --action service-run --json`、根入口 `safeclaw.ps1 verify --json`、根入口 `safeclaw.ps1 workspace --json`、根入口 `safeclaw.cmd workspace --json`、根入口 `safeclaw.cmd doctor --json`、根入口 `safeclaw.cmd service-status --limit 5 --json`、根入口 `safeclaw.ps1 service-status --limit 5 --json`、根入口 `safeclaw.cmd preflight --action ai-reason --json`、根入口 `safeclaw.ps1 preflight --action ai-reason --json`、根入口 `safeclaw.cmd preflight --action demo --json`、根入口 `safeclaw.ps1 preflight --action demo --json`、根入口 `safeclaw.ps1 service-run --json`、根入口 `safeclaw.ps1 service-retry --json`、根入口 `safeclaw.ps1 service-recover --json`、根入口 `safeclaw.cmd service-reconcile --json`、根入口 `safeclaw.ps1 service-reconcile --json`、根入口 `safeclaw.cmd service-resume --json` 与根入口 `safeclaw.ps1 service-resume --json` 的成功合同都已锁住；其中 `safeclaw.cmd service-resume --json` 与 `safeclaw.ps1 service-resume --json` 公开的 `resume-target-not-hibernated` / `resume-target-missing` 错误合同也已锁住，`safeclaw.cmd service-run --preflight --preflight-action ai-reason --json`、`safeclaw.ps1 service-run --preflight --preflight-action ai-reason --json`、根入口 `service-retry --preflight --preflight-action ai-reason --json`、根入口 `service-recover --preflight --preflight-action ai-reason --json`、根入口 `demo --preflight --preflight-action ai-reason --json`、根入口 `service-resume --preflight --preflight-action ai-reason --json`、根入口 `service-reconcile --preflight --preflight-action ai-reason --json` 的 `preflight-blocked` 阻断合同也已锁住。接下来需要回到剩余根入口真实差集盘点。
- 计划调整说明：`Slice 172` 已按用户要求把安全抽象层预留位收进 `specs/spi/` 真源目录；若后续继续安全接入，必须在这三组接口位内增量推进；若回到既有 `M1b` 主线，则继续盘点剩余根入口真实差集，不重复已闭环区域。
- budget 侧继续冻结；下一次如果再碰 budget，仍必须先指出真实 source 在哪里。
- 现在的主视图：`doctor` 看环境，`service-status` 看治理快照 + 并发/离线边界，`preflight` 看单动作门禁，组合动作错误 JSON 看浅层顶层错误字段。
- 仓库卫生侧线已补出七层台账门禁：`ledger_index_manifest.py` 负责机读索引，`check_versions.py` 负责 manifest_version / phase 口径，`check_consistency.py` 负责机读索引与文字方案映射不漂移，`check_structure.py` 负责目标路径提前落地护栏，`check_scaffold.py` 负责 legacy 阶段根台账保留，`check_ledger_alignment.py` 负责三份主台账对齐，`check_public_docs.py` 负责公开文档联动；`selfcheck.py` 已显式串起这条链路。
- 计划调整说明：本轮把第七个真实消费点落到 `check_versions.py`，优先锁住 manifest_version 与 phase 语义口径；这比只在 loader 或文档里隐含约束更有长期收益。
- 计划调整说明：本轮先补 `docs/30-方案/20-V4-reference-compliance-rebaseline-record-20260329_030242.md`，因为两份旧合规复核文档已有事实滞后；先校正当前整改基线，再继续第八个真实消费点，长期收益更高。
- 计划调整说明：纠偏快照补完后，先把它接进 `check_public_docs.py`，确保后续继续找第八个真实消费点时，不会再按旧审计的过期说法回退判断。
- 计划调整说明：在继续找第八个真实消费点前，先把 `selfcheck.py` 的 ledger policy chain 显式前置；这样即使 broad contract tests 受历史债影响，ledger 迁移护栏也不会被提前遮住。
- 计划调整说明：顺序前置后，再把这条现行链路写进 `tools/checks/README.md` 并锁进 public docs 合同，避免入口说明落后于真实门禁。
- 计划调整说明：在现行顺序稳定后，再把 `selfcheck.py` 的 ledger policy chain 抽成常量真源；这样后续若继续调整阶段，只需改单处，不再双处手写。
- 计划调整说明：本轮把第八个真实消费点落到 `.github/workflows/contracts.yml`；先让 CI 长期入口显式前置 ledger policy chain，再进 contract tests，长期收益高于继续补同层说明。
- 计划调整说明：第八个消费点落到 CI 后，本轮继续补根 README 与 Operator Playbook 两个维护入口；先让人看到的入口说明也对齐 ledger-first 顺序，再由 public docs 门禁锁住。
- 计划调整说明：本轮把第十个真实消费点落到 `tools/README.md`；先把仓库工具总入口对齐 ledger-first policy，再由 `check_public_docs.py` 锁成长期门禁，长期收益高于继续补次级说明页。
- 计划调整说明：本轮把第十一个真实消费点落到 `docs/IMPLEMENTATION_STRATEGY.md`；先让长期策略文档也显式承认 ledger-first policy，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十二个真实消费点落到 `tests/contracts/README.md`；先让测试长期入口也显式说清 Contract tests 后置于 ledger-first policy chain，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补二级索引页。
- 计划调整说明：本轮把第十三个真实消费点落到 `tests/README.md`；先让测试总入口也显式说明 contracts 测试属于 ledger-first policy 之后的阶段，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十四个真实消费点落到 `docs/README.md`；先让公开文档总入口也显式说明现行 ledger-first policy，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。
- 计划调整说明：本轮把第十五个真实消费点落到 `specs/README.md`；先让协议真源总入口也显式说明 ledger-first policy chain 再进入 Contract tests，再由 `check_public_docs.py` 锁成公开门禁，长期收益高于继续补边缘说明页。

## 三、下一候选（按优先级排序）
1. 下一刀优先选择第十六个最小真实消费点
   - 目标：优先找一个尚未消费 manifest 的真实入口继续接入，不重复包裹已有 versions、consistency、structure、scaffold、public docs、selfcheck、CI contracts workflow、tools 根入口、长期策略文档、contracts 测试入口、tests 总入口、docs 总入口与 specs 真源总入口。
   - 原因：第十五个最小切片已落到 `specs/README.md` 这个协议真源总入口，当前更需要继续扩大长期入口覆盖面，而不是在同层重复堆门禁。
   - 前提：先以 `docs/30-方案/20-V4-reference-compliance-rebaseline-record-20260329_030242.md` 作为当前合规纠偏快照，不再按旧审计里“目录锁定清单缺失”或“公开文档门禁仍直接绑定根台账”的过期说法判断现状。
2. budget 侧继续冻结
   - 前提：除非先出现真实 runtime/source，例如 mmap 路径、reservation counters、reconcile/drift 统计或可复用配置骨架；否则不再重复开 budget 面板切片。
## 四、暂不在本轮推进
- Sidecar Manager 正式恢复策略
- 财务 mmap / 预扣减
- 伪造 budget/operator 静态占位字段
- 多 Worker 并发与 scope-level mutex 大改
- 心跳 RSS / 最大服役配额

## 五、执行约束
- 优先修根因，不做表面文案补丁。
- 不改 Rust 状态模型，不新增大块架构概念。
- 若新增合同字段，必须同步文本输出、JSON 输出、README、smoke。
- 若行为变化可见，必须同步 `MVP_PROGRESS.md`、`PUSH_LOG.md` 和 `开发计划.md`。
- 台账写法要求：`MVP_PROGRESS.md`、`PUSH_LOG.md` 尽量使用中文、短句、小学生能懂；先写“做了什么”，再写“有什么用”。
- 验证顺序固定：`py_compile -> check_versions.py -> check_consistency.py -> check_structure.py -> check_scaffold.py -> check_ledger_alignment.py -> check_public_docs.py -> check_tooling_smoke.py -> check_mvp_operator_flow.py -> selfcheck.py`。

## 六、关键文件
- 蓝图基线：`01_文档/03_开发蓝图.md`
- 逻辑入口：`tools/mvp/safeclaw_mvp.py`
- 主 smoke：`tools/checks/check_tooling_smoke.py`
- 操作流：`tools/checks/check_mvp_operator_flow.py`
- 台账门禁：`tools/checks/check_versions.py`、`tools/checks/check_consistency.py`、`tools/checks/check_ledger_alignment.py`、`tools/checks/check_structure.py`、`tools/checks/check_scaffold.py`
- 用户说明：`README.md`、`tools/mvp/README.md`
- 台账：`MVP_PROGRESS.md`、`PUSH_LOG.md`

## 七、下一刀完成判据
- 先把第十六个最小真实消费点补进 manifest 兼容层；若转向显式上下文入口，先确认它不只是重复包一层已有检查器。
- 若下一刀重新回到 budget 侧，需先说明真实数据源来自哪里，以及为什么这次不再是伪合同。
- 相关文本 / JSON / smoke / docs 不回归。
## 八、后续对照规则
- 每推进一刀，先看本文件“当前状态”和“下一候选”。
- 若下一刀候选发生变化，必须先在本文件补一行“为什么改计划”；若未验真，不提前写死后续 Slice 编号。
- 若出现大面积验证漂移，先查残留验证进程与 `target/mvp/.wrapper-check.lock`。


# modules/

本目录预留给 SafeClaw 的可替换扩展实现。

当前原则：

- `specs/` 定义契约
- `modules/` 承接可替换实现
- 新能力优先在此扩展，而不是直接侵入核心语义

当前预留子目录：

- `modules/adapters/`
- `modules/memory/`
- `modules/plugins/`
- `modules/repair_plans/`
